# --- Core (required — app runs fully on these) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
PyJWT==2.10.1
passlib==1.7.4
python-dotenv==1.0.1
requests==2.32.3
numpy==2.2.1
scikit-learn==1.6.0

# --- AI-enhanced (optional — app degrades gracefully if any fail to install) ---
# Gemini generation + embeddings
google-genai==1.9.0
# FAISS vector store (falls back to TF-IDF if unavailable)
faiss-cpu==1.9.0.post1
# LangGraph report pipeline (falls back to sequential pipeline if unavailable)
langgraph==0.2.60
